import { MyWebShopValidators } from './my-web-shop-validators';

describe('MyWebShopValidators', () => {
  it('should create an instance', () => {
    expect(new MyWebShopValidators()).toBeTruthy();
  });
});
